# Agentic Trust Foundation V1 Final Release — 2026-09

## Release status

**ATF Protocol V1 FINAL — protocol + schemas + conformance + reference implementation**

ATF V1 defines the open trust/delegation/authorization layer used by independent agentic systems. It is not a centralized global trust service.

## V1 normative surface

1. Overview
2. Terminology
3. Identity
4. Credentials
5. Delegation
6. Authorization
7. Capabilities
8. Policy
9. Trust
10. Consent
11. Revocation
12. Provenance
13. Audit
14. Human Approval
15. Discovery
16. Security
17. Privacy
18. Errors
19. Versioning
20. Conformance

The normative specifications live under `specs/v1/`. JSON Schemas live under `schemas/v1/`.

## V1 authorization semantics

Authorization outcomes are exactly:

- ALLOW
- DENY
- REQUIRE_HUMAN

Authentication is not authorization. Missing, invalid, expired, revoked, or indeterminate required authority fails closed.

Delegation is bounded authority and cannot silently exceed the authority held by the delegator.

## Agent-Pay boundary

ATF publishes the normalized semantic authority contract consumed by Agent-Pay. Agent-Pay owns financial policy and execution; ATF owns general authority.

V1 intentionally does not freeze one universal ATF credential or signature wire format. Implementations may use different credential mechanisms if they preserve the normative semantics and applicable conformance requirements.

## Conformance

V1 includes:

- machine-readable core vectors;
- Agent-Pay cross-repository vectors;
- JSON Schema validation;
- deterministic reference authorization semantics;
- CI conformance workflow.

## Security boundary

Credential verification, revocation and delegation-chain validation are normative requirements. A verifier must fail closed when a required security fact cannot be established.

## Non-goals

V1 does not require:

- one hosted ATF service;
- one identity provider;
- one credential/token format;
- one agent runtime;
- one payment provider;
- centralized reputation;
- mandatory SaaS infrastructure.

## Release acceptance

ATF V1 is the stable protocol baseline. Breaking semantic changes require a new protocol version or an explicit versioned extension/profile.
