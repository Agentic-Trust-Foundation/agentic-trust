# 19. Versioning

## Status
Normative — ATF Protocol v1.

Protocol messages and conformance artifacts MUST identify the protocol major version. Within a major version, implementations MUST preserve the defined semantic contract.

Breaking semantic changes require a new major protocol version. Extensions MUST be explicitly identified and MUST NOT silently change the meaning of existing required fields or decision outcomes.
